import sys

def main(argv=None):
    if argv is None:
        argv = sys.argv

    say_salam(name_from,name_to)
   
    return 0

def say_salam(name_from,name_to):
    print(f"salam from {name_from} to my dear {name_to}"
